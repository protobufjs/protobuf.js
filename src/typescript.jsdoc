/**
 * Constructor type.
 * @typedef Constructor
 * @template T extends object
 * @type {Function}
 * @tstype { new(...params: any[]): T }
 */

/**
 * Properties type.
 * @typedef Properties
 * @template T extends object
 * @type {Object.<string,*>}
 * @tstype { [P in keyof T]?: T[P] } & { [key: string]: any }
 */
